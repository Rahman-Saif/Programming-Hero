const glass={
    name:"glass",
    color:"golden",
    price:12,
    isCleaned:true
}

const keys=Object.keys(glass);
// console.log(keys);
const pair=Object.entries(glass);
console.log(pair);
